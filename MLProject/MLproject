name: Latihan Credit Scoring

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      dataset: {type: string, default: "heart_preprocesing.csv"}
    command: "python modelling.py {dataset}"
